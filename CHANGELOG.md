# سجل التغييرات

قسم كل إصدار هنا يصبح ملاحظات الإصدار في صفحة Releases، ويظهر في نافذة «عرض التفاصيل» داخل لوحة تحكم ووردبريس. عند نشر إصدار جديد أضف قسمه في الأعلى بالعنوان `## رقم-الإصدار`.

## 1.11.0

- **التسجيل بلا انتظار بريد:** يختار الزائر كلمة مروره في نموذج التسجيل، فيدخل مباشرة ويعود إلى الصفحة التي ضغط منها «حساب جديد» (صفحة اللعبة، أو قسم التقييم أو التعليقات فيها) مع رسالة ترحيب. كان التسجيل يرسل رابطاً بالبريد لتعيين كلمة المرور، فلا يدخل الزائر قبل فتحه، ويتعطل التسجيل كله إن كان بريد الموقع لا يصل. الخيار في «الألعاب ← الإعدادات ← تسجيل الأعضاء» ومفعّل افتراضياً، وإطفاؤه يعيد طريقة ووردبريس كما هي.
- **إعدادات الحساب داخل «حسابي»:** الاسم الظاهر والبريد وكلمة المرور تتغير من صفحة «حسابي» نفسها، بدل صفحة الملف الشخصي في لوحة التحكم وخياراتها التي لا تعني العضو. تغيير البريد أو كلمة المرور يطلب كلمة المرور الحالية، ويصل تنبيه بالتغيير إلى البريد السابق، وتغيير كلمة المرور يُخرج الحساب من الأجهزة الأخرى. و«ملفي الشخصي» في قائمة الحساب صار «إعدادات الحساب» ويفتح هذا القسم.
- تحت «اسم المستخدم» في نموذج التسجيل: أحرف إنجليزية وأرقام، والاسم الظاهر بالعربية يُختار من «حسابي». ووردبريس يرفض الحروف العربية في اسم المستخدم دون أن يقول ما المسموح.
- لا يُنشأ حساب قبل اختيار كلمة مرور من 8 أحرف على الأقل، وحقل مخفي يصدّ كثيراً من برامج التسجيل الآلية. المدير يصله إشعار «عضو جديد» كالمعتاد.
- رابط الدخول في صفحة التسجيل، ورابط التسجيل في صفحة الدخول، يحفظان الصفحة التي تعود إليها بعدهما.
- في الوضع الداكن كانت صناديق الرسائل في صفحتي الدخول والتسجيل بيضاء ونصها فاتح لا يُقرأ، وأيقونة إظهار كلمة المرور داكنة على حقل داكن.

## 1.10.0

- **إضافة الألعاب أسهل:** عند رفع ملف اللعبة يُختار نظامها تلقائياً من امتداد الملف (‎.nes ← NES، ‎.gba ← GBA…)، ويُكتب اسمها من اسم الملف إن كان العنوان فارغاً. الامتدادات المشتركة بين عدة أنظمة (‎.bin ‎.cue ‎.iso ‎.zip) تبقى لاختيارك مع تنبيه.
- **قائمة «جاهزية اللعبة»** داخل صندوق النشر فوق زر «نشر» تبيّن أثناء التعديل: ملف اللعبة، والنظام، وتطابق الملف مع النظام، ثم الغلاف والوصف المختصر.
- **تأكيد قبل نشر لعبة لن تعمل:** إن كانت بلا ملف أو بلا نظام أو بملف لا يناسب نظامها، يسألك قبل النشر بدل أن تُنشر معطّلة.
- لعبة حُفظت بلا نظام وملفها يدل على نظام واحد: يختاره الموقع عند الحفظ ويخبرك، حتى بدون JavaScript.
- قائمة الألعاب تذكر سبب عدم عمل اللعبة («لم يُرفع ملفها»، «لم يُختر نظامها»…) بدل «غير قابلة للتشغيل».
- صفحة اللعبة تقول «هذه اللعبة غير جاهزة للتشغيل بعد» بدل «ملف هذه اللعبة لم يُرفع بعد» حين يكون الملف مرفوعاً والنظام ناقصاً. ومن يحرّر اللعبة يرى ما ينقصها ورابطاً لإكماله.

## 1.9.0

- **اللعب على الجوال بملء الشاشة:** بعد «ابدأ اللعب» تملأ اللعبة شاشة الجوال كلها، عمودياً وأفقياً، وعلى iPhone أيضاً. زر × في الزاوية أو زر الرجوع في المتصفح يعيدك إلى صفحة اللعبة واللعبة ما زالت تعمل، وزر «ملء الشاشة» يعيدك إليها. كانت اللعبة تكبر داخل الصفحة فتبقى أزرار اللمس أسفل حافة الشاشة ولا تنتقل الصفحة إليها.
- **قوائم المحاكي بعربية سليمة:** ترجمة EmulatorJS العربية آلية وفيها أخطاء فادحة: «Load State» ← «الدولة الحمل»، و«Tab» ← «فاتورة غير مدفوعة»، و«PAL» ← «صديق». وكان زرّا اللمس A وB مكتوبين «أ» و«ب». صارت الإضافة ترفق ترجمتها لكل القوائم والرسائل وأزرار التحكم، وتعمل بدون إنترنت أيضاً.
- **إصلاح انهيار الصفحة:** إن فشل تنزيل ملف اللعبة (انقطاع الاتصال مثلاً) كان المحاكي يُكمل التشغيل فينهار تبويب المتصفح كله. تظهر الآن رسالة «تعذّر تنزيل اللعبة…»، وزر «إعادة التشغيل» يعيد المحاولة.
- «إنهاء اللعب» من قائمة المحاكي كان يترك شاشة سوداء، وصار يعيد شاشة «ابدأ اللعب» بعد أن يحفظ المحاكي اللعبة. وفي النافذة المستقلة يعود إلى صفحة اللعبة.
- لمس شاشة اللعبة لم يعد يكبّر الصفحة أو يحرّكها.

## 1.8.0

- **صفحات أسرع:** غلاف كل لعبة وملفها كانا يُقرآن من قاعدة البيانات لكل بطاقة على حدة؛ صارت تُجلب للقائمة كلها دفعة واحدة. في موقع تجريبي فيه 24 لعبة: المكتبة من 66 استعلاماً إلى 24، والرئيسية من 78 إلى 58، وصفحة اللعبة من 48 إلى 36.
- أغلفة الصف الأول في المكتبة تُحمَّل فوراً بدل انتظار التمرير. وأغلفة شاشة «الملتي كارت» تُحمَّل بعد اكتمال الصفحة، وعلى الشاشات الكبيرة فقط (على الجوال لا تظهر المعاينة أصلاً).
- **الفلاتر:** البحث والترتيب في الصف الأول، ثم النظام والنوع واللاعبون والحالة بعرض يتسع لأسماء الأجهزة الطويلة (كان «Nintendo Entertainment System» يظهر مقطوعاً). النتائج تتحدث أثناء كتابة البحث، وعنوان الصفحة وشريط الأنظمة يتبعان النظام المختار، وزرا الرجوع والتقدم في المتصفح يعيدان الفلاتر كما كانت.
- على الجوال تُطوى الفلاتر الأربع خلف زر «الفلاتر» مع عدد المختار منها، فتظهر الألعاب في أول الشاشة بدل نصف شاشة من القوائم. و«إزالة الفلاتر» صار بجانب عدد النتائج.
- **البطاقات:** النوع والتقييم (نجمة ورقم) في المكان نفسه في كل بطاقة، بدل النجوم في بعضها والنوع في غيرها. والخرطوشة ترتفع قليلاً عند المرور عليها أو الوصول إليها بلوحة المفاتيح.
- **الرئيسية:** رف «حُدّثت مؤخراً» يعرض ما عُدّل بعد نشره فعلاً ولا يكرر ألعاب «وصلت حديثاً». بطاقات الأنظمة بارتفاع واحد والعدد في أسفلها، وقائمة الأنظمة في التذييل بعمودين.
- **إمكانية الوصول:** النص الرمادي أغمق قليلاً ليبلغ نسبة التباين المطلوبة، وقارئات الشاشة تعلن عدد النتائج بعد كل فلترة.
- مسافة كانت ناقصة بين الألعاب و«مقالات وصفحات» في نتائج البحث.
## 1.7.2

- سد تجاوز حماية ملفات الألعاب عبر عامل الخدمة: فتح الملف كصفحة يُرفض حتى إن كان مخزّناً، ولا تستبدل الذاكرة ردود الرفض من الخادم بنسخة ناجحة. تُزال النسخة المخزنة عند رد 401 أو 403 أو 404 أو 410؛ يبقى الرجوع إليها متاحاً عند انقطاع الشبكة.
- ربط روابط المشغّل بجلسة المتصفح والحساب مع Cookie من نوع HttpOnly وSameSite=Lax وSecure على HTTPS. نسخ الرابط وحده إلى متصفح آخر لا يسمح بتحميله، وتُرفض الطلبات مجهولة المصدر والأساليب غير GET وHEAD.
- منع ذاكرة HTTP من تجاوز التحقق على الخادم، ومنع كشف رابط عام عند فشل حماية مرفق أو فقدان ملفه. فشل تسجيل مسار الملف يعيد الملف إلى موضعه السابق، وتُراجع ملفات المنع عند الترقية.
- توضيح أن الاسم العشوائي لا يغني عن قاعدة منع على nginx، وأن استخراج الملف من جهاز اللاعب يظل ممكناً لأن المحاكي يحتاجه للتشغيل.
- اختبارات إضافية لعزل الجلسات وانتهاء الروابط وكلمات المرور وحالة النشر وإيقاف التنزيل، وفشل حماية المرفقات، ورفض الروابط المخزنة في Chromium.

بعد التحديث أعد تحميل صفحات اللعب المفتوحة للحصول على روابط جلسة جديدة. يجب السماح بملف Cookie الوظيفي للمشغّل. عند انتهاء صلاحية رابط أثناء الاتصال أعد فتح صفحة اللعب.

## 1.7.1

- **حفظ سحابي أكثر أماناً:** لا تُعتبر مزامنة إغلاق الصفحة ناجحة حتى يؤكدها الخادم؛ يبقى التقدّم المحلي قابلاً لإعادة الرفع إذا فشل الإرسال. تُحفظ حالة المحاكي محلياً عند فشل الخادم أيضاً.
- منع طلبات الحفظ المتزامنة من استبدال بيانات بعضها، والتحقق من نسخة SRAM السابقة قبل قبول تحديث جهاز آخر. عند اختلاف التقدّم على جهازين يظهر اختيار واضح بينهما، ولا تُحذف الملفات السابقة إذا فشل تحديث قاعدة البيانات.
- التحقق من صلاحية المرفق وامتداده عند اختيار ملف اللعبة من المحرر أو REST أو واجهة metadata؛ لا تُنقل الصور والمستندات أو ملفات مستخدم آخر دون صلاحية.
- تفعيل حقول الألعاب في REST مع تنقية موحدة للقيم والنصوص والروابط.
- شارة اللعب دون إنترنت تتأكد من وجود ملف اللعبة وملفات التشغيل والصفحات في الذاكرة، وتُخفى عند تغيّر إصدار اللعبة أو فقدان أحد الملفات. تعطيل PWA يزيل تسجيله وذاكرته، وتبديل الحساب يمسح الصفحات الشخصية المحفوظة.
- إضافة اختبارات للحفظ، والصلاحيات وREST على WordPress، والتشغيل دون اتصال في Chromium إلى بوابة إصدار الحزم.

## 1.7.0

- **حماية ملفات الألعاب من التنزيل:** الملف المرفوع يُنقل إلى مجلد مغلق باسم عشوائي، ولا يصل إليه إلا المشغّل برابط مؤقت يتغير كل بضع ساعات ولا يعمل إن فُتح في المتصفح أو طلبه موقع آخر. ولا يظهر في مكتبة الوسائط العامة. الملفات الحالية تُنقل تلقائياً عند التحديث، واللعب بدون إنترنت يعمل كما هو.
- زر «تنزيل الملف» (للألعاب التي تسمح بتنزيلها) يرسل الملف دون كشف مكانه.
- محرر اللعبة يعرض حالة الملف («محمي»)، وصفحة الإعدادات تشرح الحماية مع قاعدة إضافية لخوادم nginx.
- الملفات المضافة بحقل «رابط مباشر» (على خادم آخر) لا تُحمى. ولا يمكن منع التنزيل كلياً لأن اللعبة تعمل داخل المتصفح، لكن الطرق السهلة مغلقة.

## 1.6.2

- **لا «تمدد» عند فتح الصفحة أو تحديثها:** كانت الصفحة تظهر لحظةً بخط الجهاز (وهو أعرض) ثم تنكمش عند وصول خطوط القالب، فتتحرك القائمة والأزرار ويُعاد ترتيب النص. صارت الخطوط تُطلب مع أول سطر في الصفحة فتصل قبل ظهورها عادةً، ونصوص الشاشات (خط البكسل) لا تظهر بخط بديل.
- **الرد على التعليقات دون إعادة تحميل الصفحة:** بعد «نشر التعليق» تبقى الصفحة في مكانها ويظهر ردك تحت التعليق الذي رددت عليه، بدل أن تُحمَّل الصفحة من أعلاها ثم تقفز إلى الرد. وإن رُفض التعليق (مكرر أو سريع جداً) يظهر السبب تحت الزر ويبقى نصك كما هو.
- **إصلاح:** زر «رد» وعنوان «رد على…» في نموذج التعليق كانا يظهران بالإنجليزية (Reply to) إذا كانت لغة ووردبريس غير العربية أو لم تُثبَّت ترجمتها.

## 1.6.1

- **صفحة اللعبة بتصميم جديد:** ترويسة بعرض الصفحة فيها خرطوشة اللعبة واسمها ووصفها وتقييمها وعدد مرات لعبها، وأزرار المفضلة والمشاركة والتنزيل في مكان ظاهر بدل أسفل التفاصيل.
- المشغّل وبجانبه التقييم والتفاصيل، بلا فجوة فارغة تحت المشغّل.
- أزرار لوحة المفاتيح تحت الشاشة مباشرة بدل جدول في آخر الصفحة.
- **توزيع التقييمات:** صندوق التقييم يعرض عدد التقييمات لكل درجة من 5 نجوم إلى نجمة، ويتحدث فور تقييمك.
- كل لقطات اللعبة ظاهرة في شبكة بدل شريط يقصّ آخرها، ويوميات التطوير في خط زمني مختصر بلون نظام اللعبة.
- على الجوال: الخرطوشة بجانب الاسم، واللقطات في عمودين، ولا يظهر زر «وضع العرض الواسع» لأن الشاشة تملأ العرض أصلاً.
- لعبة لم يُرفع ملفها بعد لا تعرض إرشادات التشغيل ولا أزرار التحكم.

## 1.6.0

- **تحديثات تلقائية من GitHub:** كل إصدار جديد يظهر في «لوحة التحكم ← التحديثات» للإضافة والقالب معاً، ويُثبَّت بضغطة واحدة كأي إضافة من wordpress.org. نافذة «عرض التفاصيل» تعرض ما الجديد.
- **خطوط مستضافة مع القالب:** Handjet وIBM Plex Sans Arabic صارتا جزءاً من القالب بدل خوادم Google: تحميل أسرع، وتعملان بدون إنترنت في تطبيق الويب، ولا تُرسَل عناوين الزوار لطرف ثالث.
- **إصلاح:** سجل التحديثات في رسائل «إصدار جديد» للمتابعين كان يظهر مقطوعاً ومشوّهاً في كل سطر فيه حرف «م».
- **إصلاح:** تصدير الإحصائيات (CSV) كان يطلق تحذيراً مع كل صف على PHP 8.4.
- **إمكانية الوصول:** تباين أوضح لنص التذييل وللزر الأحمر في الوضع الداكن، وعنوان رئيسي لصفحة «غير موجودة»، واسم مميز لكل نموذج بحث.
- **الخصوصية:** عند تسجيل الخروج أو الدخول بحساب آخر تُحذف الصفحات المحفوظة للعمل بدون إنترنت، فلا تظهر صفحات الحساب السابق على جهاز مشترك.
- تواريخ صفحة «حسابي» بتوقيت الموقع.
- اختبار آلي مع كل تعديل: تثبيت الإضافة والقالب على ووردبريس حقيقي (PHP 7.4 و8.4) وفتح صفحات الموقع، ولا يُنشر إصدار إلا بعد نجاحه.

## 1.5.1

- ربط الإضافة والقالب بمستودع GitHub، ومنع ووردبريس من عرض «تحديثات» لإضافات أو قوالب أخرى بالاسم نفسه في wordpress.org.

## 1.5

- أُزيلت النسخة الإنجليزية، وتُحذف بياناتها المتبقية من قاعدة البيانات عند الترقية.

## 1.4

- صفحة **الإحصائيات**: مرات اللعب والتقييمات والمفضلة والتعليقات والتنزيلات والأعضاء الجدد لكل فترة، مع المقارنة بالفترة السابقة وتصدير CSV.
- رف «رائجة هذا الأسبوع» في الرئيسية، وترتيب «الرائجة الآن» في المكتبة.

## 1.3

- إشعارات المتابعين: من أضاف لعبة لمفضلته يُبلَّغ بإصداراتها الجديدة وتدويناتها، داخل الموقع وبالبريد.
- تطبيق ويب قابل للتثبيت، والألعاب التي شُغّلت مرة تعمل بعدها بدون إنترنت.

## 1.2

- يوميات التطوير: تدوينات مرتبطة بالألعاب تظهر في صفحاتها وفي الرئيسية.
- مزامنة حفظ اللعبة الداخلي (SRAM) بين أجهزة العضو، والاحتفاظ بعدة حفظات لكل لعبة.

## 1.1

- ميزات الأعضاء: الحفظ السحابي، والمفضلة، وصفحة «حسابي»، والتعليقات للأعضاء المسجّلين فقط.

## 1.0

- الإصدار الأول: مكتبة الألعاب والأنظمة والأنواع، ومشغّل EmulatorJS، والتقييم بالنجوم.
